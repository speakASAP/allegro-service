apiVersion: v1
kind: Service
metadata:
  name: allegro-imports
  namespace: statex-apps
  labels:
    app: allegro-imports
    managed-by: k8s-migration
spec:
  type: ClusterIP
  selector:
    app: allegro-imports
  ports:
    - protocol: TCP
      port: 3406
      targetPort: 3406
      name: app
